#version 140

	in vec2 uvPassed;
	out vec4 fragmentColor; 
	uniform sampler2D textureSampler;
	uniform mat4 colorTransform;

	void main() {
	float tx2 = uvPassed.x;
	float ty2 = uvPassed.y;
	vec2 uvBiased = vec2(tx2, ty2);
		fragmentColor = colorTransform * texture(textureSampler, uvBiased); 
	}